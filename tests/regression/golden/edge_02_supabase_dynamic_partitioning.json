[
  {
    "rule_id": "analyzer-blind-on-dynamic-sql",
    "severity": "warning",
    "file": "edge_02_supabase_dynamic_partitioning.sql",
    "line": 59,
    "column": 52,
    "message": "CREATE FUNCTION app.create_chats_partition contains EXECUTE \u2014 the analyzer cannot see the dynamic SQL that will actually run.",
    "help": "PL/pgSQL bodies containing EXECUTE generate SQL at runtime from string expressions. Common patterns: EXECUTE format('CREATE TABLE %I ...', tbl_name), EXECUTE 'SELECT ' || dynamic_str. The analyzer can see the outer DO block / CREATE FUNCTION but not the SQL that EXECUTE will produce at runtime. Possible concerns: (a) format()'s %s vs %L vs %I confusion (use %I for identifiers, %L for literals \u2014 %s in a DDL context is a SQL-injection vector), (b) the generated DDL itself could be a problem rule we'd otherwise catch. Read the EXECUTE arguments by hand.",
    "suggested_fix": null
  },
  {
    "rule_id": "analyzer-blind-on-dynamic-sql",
    "severity": "warning",
    "file": "edge_02_supabase_dynamic_partitioning.sql",
    "line": 73,
    "column": 3,
    "message": "CREATE FUNCTION app.create_chat_messages_partition contains EXECUTE \u2014 the analyzer cannot see the dynamic SQL that will actually run.",
    "help": "PL/pgSQL bodies containing EXECUTE generate SQL at runtime from string expressions. Common patterns: EXECUTE format('CREATE TABLE %I ...', tbl_name), EXECUTE 'SELECT ' || dynamic_str. The analyzer can see the outer DO block / CREATE FUNCTION but not the SQL that EXECUTE will produce at runtime. Possible concerns: (a) format()'s %s vs %L vs %I confusion (use %I for identifiers, %L for literals \u2014 %s in a DDL context is a SQL-injection vector), (b) the generated DDL itself could be a problem rule we'd otherwise catch. Read the EXECUTE arguments by hand.",
    "suggested_fix": null
  },
  {
    "rule_id": "analyzer-blind-on-dynamic-sql",
    "severity": "warning",
    "file": "edge_02_supabase_dynamic_partitioning.sql",
    "line": 94,
    "column": 3,
    "message": "CREATE FUNCTION app.copy_chats_partition contains EXECUTE \u2014 the analyzer cannot see the dynamic SQL that will actually run.",
    "help": "PL/pgSQL bodies containing EXECUTE generate SQL at runtime from string expressions. Common patterns: EXECUTE format('CREATE TABLE %I ...', tbl_name), EXECUTE 'SELECT ' || dynamic_str. The analyzer can see the outer DO block / CREATE FUNCTION but not the SQL that EXECUTE will produce at runtime. Possible concerns: (a) format()'s %s vs %L vs %I confusion (use %I for identifiers, %L for literals \u2014 %s in a DDL context is a SQL-injection vector), (b) the generated DDL itself could be a problem rule we'd otherwise catch. Read the EXECUTE arguments by hand.",
    "suggested_fix": null
  },
  {
    "rule_id": "analyzer-blind-on-dynamic-sql",
    "severity": "warning",
    "file": "edge_02_supabase_dynamic_partitioning.sql",
    "line": 116,
    "column": 3,
    "message": "CREATE FUNCTION app.copy_chat_messages_partition contains EXECUTE \u2014 the analyzer cannot see the dynamic SQL that will actually run.",
    "help": "PL/pgSQL bodies containing EXECUTE generate SQL at runtime from string expressions. Common patterns: EXECUTE format('CREATE TABLE %I ...', tbl_name), EXECUTE 'SELECT ' || dynamic_str. The analyzer can see the outer DO block / CREATE FUNCTION but not the SQL that EXECUTE will produce at runtime. Possible concerns: (a) format()'s %s vs %L vs %I confusion (use %I for identifiers, %L for literals \u2014 %s in a DDL context is a SQL-injection vector), (b) the generated DDL itself could be a problem rule we'd otherwise catch. Read the EXECUTE arguments by hand.",
    "suggested_fix": null
  },
  {
    "rule_id": "analyzer-blind-on-dynamic-sql",
    "severity": "warning",
    "file": "edge_02_supabase_dynamic_partitioning.sql",
    "line": 138,
    "column": 3,
    "message": "CREATE FUNCTION app.index_and_attach_chats_partition contains EXECUTE \u2014 the analyzer cannot see the dynamic SQL that will actually run.",
    "help": "PL/pgSQL bodies containing EXECUTE generate SQL at runtime from string expressions. Common patterns: EXECUTE format('CREATE TABLE %I ...', tbl_name), EXECUTE 'SELECT ' || dynamic_str. The analyzer can see the outer DO block / CREATE FUNCTION but not the SQL that EXECUTE will produce at runtime. Possible concerns: (a) format()'s %s vs %L vs %I confusion (use %I for identifiers, %L for literals \u2014 %s in a DDL context is a SQL-injection vector), (b) the generated DDL itself could be a problem rule we'd otherwise catch. Read the EXECUTE arguments by hand.",
    "suggested_fix": null
  },
  {
    "rule_id": "analyzer-blind-on-dynamic-sql",
    "severity": "warning",
    "file": "edge_02_supabase_dynamic_partitioning.sql",
    "line": 182,
    "column": 3,
    "message": "CREATE FUNCTION app.index_and_attach_chat_messages_partition contains EXECUTE \u2014 the analyzer cannot see the dynamic SQL that will actually run.",
    "help": "PL/pgSQL bodies containing EXECUTE generate SQL at runtime from string expressions. Common patterns: EXECUTE format('CREATE TABLE %I ...', tbl_name), EXECUTE 'SELECT ' || dynamic_str. The analyzer can see the outer DO block / CREATE FUNCTION but not the SQL that EXECUTE will produce at runtime. Possible concerns: (a) format()'s %s vs %L vs %I confusion (use %I for identifiers, %L for literals \u2014 %s in a DDL context is a SQL-injection vector), (b) the generated DDL itself could be a problem rule we'd otherwise catch. Read the EXECUTE arguments by hand.",
    "suggested_fix": null
  }
]
